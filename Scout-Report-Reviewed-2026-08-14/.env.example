PORT=3000

HOST=localhost

NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432

DB_NAME=scout_report

DB_USER=postgres

DB_PASSWORD=

JWT_SECRET=replace-with-a-long-random-secret
JWT_TTL_SECONDS=28800

# Set both to true for managed PostgreSQL instances that require TLS.
DB_SSL=false
DB_SSL_REJECT_UNAUTHORIZED=true
