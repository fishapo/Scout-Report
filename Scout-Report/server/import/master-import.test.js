"use strict";const test=require("node:test");const assert=require("node:assert/strict");const mapper=require("./master-import");
test("master schema inventories 38 source columns",()=>{assert.equal(mapper.schema.columns.length,38);assert.equal(mapper.schema.columns.find(c=>c.source_heading==="White fly_adult").database_key,"white_fly_adult")});
test("master row maps source headings",()=>{const r=mapper.validateSourceRow({WEEK:4,Farm:"6",GH:"4b","Inpl. wk-year":"40/25",Crop:"Rose",Variety:"Senorita",Thrips_Larvae:12,"White fly_adult":4});assert.deepEqual(r.errors,[]);assert.equal(r.normalized.thrips_larvae,12);assert.equal(r.normalized.white_fly_adult,4)});
test("master row rejects negative observations",()=>{assert.throws(()=>mapper.validateSourceRow({WEEK:4,Farm:"6",Crop:"Rose",Variety:"V",Thrips_Larvae:-1}),/cannot be negative/)})
test("master observations classify pest disease stress",()=>{const r=mapper.toCanonicalHeader(mapper.validateSourceRow({WEEK:4,Farm:"6",Crop:"Rose",Variety:"V",Thrips_Larvae:2,Leafspot_Black:3,Others:"Crowngalls"}).normalized);assert.equal(r.observations.pests[0].sourceKey,"thrips_larvae");assert.equal(r.observations.diseases[0].sourceKey,"leafspot_black");assert.equal(r.observations.stress[0].sourceKey,"others")});
